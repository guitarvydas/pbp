#!/bin/bash

######### commands specific to this project #############
# env vars that may be used in this script:
#  $PATH
#  $TOOLWD ($(pwd))
#  $CALLERPATH
#  $PYTHONPATH
#  $TARGET

# use-cases
# 1. build a transmogrifier
# 2.       <deprecated>
# 3. use T2T to transmogrify some text to some other text

# if you want to build a use-case 3 tool, then comment out use-case 1 below and uncomment the appropriate use-case

# # use-case 1 (build a drawware tool that is like a compiler that transmogrifies a custom language to some existing language (say custom-to-Python)
das2json "${TRANSMOGRIFIER}.drawio"
python main.py "${TARGET}.${SRC_EXTENSION}" main "${TRANSMOGRIFIER}.drawio.json" | splitoutputs
checkfailure
mv "out.${DEST_EXTENSION}" "${TARGET}.${DEST_EXTENSION}"
rm "${TRANSMOGRIFIER}.drawio.json"


# # use-case 3 just use the T2T tool to convert some text to some other text ("transmogrify")
# # if you only want to use the T2T tool, then comment out the above and use the following:
# output=/dev/stdout
# ./pbp/t2t "${TARGET}" <"**modify this (the source file to be transmogrified)**" >$output


############################




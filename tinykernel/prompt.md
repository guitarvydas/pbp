I want a big block of utf-8 characters. A different array of descriptors. Each descriptor contains 2 items - a 16 bit length and a 32-bit index into the block. The descriptors describe an implementation of utf-8 strings. The descriptor array has up to 65535 entries and can be indexed by a 16 bit unsigned integer. the block of characters can be indexed by a 32-bit unsigned integer.
